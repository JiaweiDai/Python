# There are two main kind output : expressions and print()
# You also could use write() output into a file, standard output sys.stdout.
# str.format() to format output
# repr() or str() turn output to string.
# str() return an easy reading expression.
# repr() produce an expression for interpreter easy read, paremeter could be any object.

s = "Hello Python!"
print(s)
print(str(s))
print(repr(s))
